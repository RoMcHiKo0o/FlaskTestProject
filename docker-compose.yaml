version: "3"

services:
  api:
    restart: always
    build:
      context: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
#    depends_on:
#      - db
#  db:
#    image: mongo:latest
#    restart: always
#    ports:
#      - "27017:27017"

